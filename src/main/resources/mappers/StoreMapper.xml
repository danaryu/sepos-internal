<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinc.sepos.internal.mapper.StoreMapper">
    <select id="getStoreList" resultType="java.util.HashMap">
        SELECT STRCD_HQ
             , STR_NM
          FROM MT_STORE
    </select>
</mapper>